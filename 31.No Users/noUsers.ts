//Add an if test to make sure the list of users is not empty.

// let usernames: string[] = [];

// if(usernames.length === 0){
//     console.log("We need to find some users!");
    
// } else {
//     // Greet Users 
// }

// // Removing all usernames ensures the message "We need to find some users!" is printed.

let usernames: string[] = ['Admin', 'tom','john','thomas','james'];

if (usernames.length === 0) {
    console.log("We need to find some users!");

  } 
  else {
    console.log("All username have been removed");
  };
